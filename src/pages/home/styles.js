import styled from '@emotion/styled';
import { ContentContainer } from 'src/styles';

export const HomeContainer = styled(ContentContainer)`
  display: flex;
  justify-content: center;
  align-items: center;
`;

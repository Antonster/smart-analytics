import styled from '@emotion/styled';

export const Footer = styled.footer`
  width: 100%;
  padding: 24px 50px;
  color: rgba(0, 0, 0, 0.85);
  font-size: 14px;
  background: #f0f2f5;
  text-align: center;
`;
